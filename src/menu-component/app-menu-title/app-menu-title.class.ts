import { AppMenuItem } from '../app-menu-item/app-menu-item.model';

export class AppMenuTitle implements AppMenuItem {
  label: string = '';
  style: string;
  closed: boolean = false;

  // constructor() {
  // }

}
